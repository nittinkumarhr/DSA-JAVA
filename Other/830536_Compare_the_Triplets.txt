// Problem: #830536 - Compare the Triplets
// Difficulty: Authorshafaetdifficultyeasymax score10submitted by2251812
// Topic: Warmup, Algorithms
// Runtime: N/A
// Memory: N/A
// Date: 24 Jul 2026
// HackerRank: https://www.hackerrank.com/challenges/compare-the-triplets/problem

public static List<Integer> compareTriplets(List<Integer> a, List<Integer> b) {
     */
     *  2. INTEGER_ARRAY b
     *  1. INTEGER_ARRAY a
     * The function accepts following parameters:
     * The function is expected to return an INTEGER_ARRAY.
     *
     * Complete the 'compareTriplets' function below.
    /*

class Result {
        List<Integer> scores = new ArrayList<>(Arrays.asList(0,0));
        for (int i = 0; i < 3; i++) {
            int alice = a.get(i);
            int bob = b.get(i);
            
            if (alice > bob) {
                scores.set(0, scores.get(0) + 1);
            } else if (alice < bob) {
                scores.set(1, scores.get(1) + 1);
            }
        }
        return scores;
    }